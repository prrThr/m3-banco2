<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Average Salary Page</title>
  </head>
  <body>
    <h1>Average Salary by Department</h1>

    <div id="resultsContainer">
      <!-- Aqui você pode iterar sobre os resultados e exibi-los -->
      <p>Total Departments: <%= data.departments.length %></p>
      
      <!-- Exemplo de iteração -->
      <% data.departments.forEach(department => { %>
        <p>Department: <%= department.dept_no %></p>
        <p>Total Salary: <%= department.totalSalary %></p>
        <p>Count: <%= department.count %></p>
        <hr />
      <% }); %>
    </div>
  </body>
</html>
